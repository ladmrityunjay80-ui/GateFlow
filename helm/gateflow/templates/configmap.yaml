apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gateflow.fullname" . }}-config
  labels:
    {{- include "gateflow.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key | upper }}: {{ $value | quote }}
  {{- end }}
